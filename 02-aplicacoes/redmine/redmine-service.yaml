apiVersion: v1
kind: Service
metadata:
  name: redmine-service
  labels:
    name: redmine-service
spec:
  selector:
    app: redmine
  #type: NodePort
  ports:
    - name: http
      port: 80
      targetPort: 3000
